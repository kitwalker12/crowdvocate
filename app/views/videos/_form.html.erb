<%= semantic_form_for [@project, @video] do |f| %>
  <%= f.inputs :name => "Video Details" do %>
    <%= f.semantic_errors :url %>
    <%= f.input :url %>
  <% end %>
    <%= f.actions :submit, :cancel %>
<% end %>