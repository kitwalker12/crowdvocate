<%= semantic_form_for [@project,@event,@comment] do |f| %>
  <%= f.inputs :name => "Add Comment" do %>
    <%= f.semantic_errors *f.object.errors.keys %>
    <%= f.input :body, :as => :text %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit, :label => "Submit" %>
  <% end %>
<% end %>