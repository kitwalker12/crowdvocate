<%= semantic_form_for [@project, @event] do |f| %>
  <%= f.inputs :name => "Event Details" do %>
    <%= f.semantic_errors :body %>
    <%= f.input :body %>
  <% end %>
    <%= f.actions :submit, :cancel %>
<% end %>